{% extends '::layout.html.twig' %}

{% block title %}CfpTalk edit{% endblock %}

{% block body %}
<form action="{{ path('CfpCfpBundle_update_cfp_talk', { 'cfp_id': 1, 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('CfpCfpBundle_show_my_cfp_talks', {'cfp_id': 1}) }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('CfpCfpBundle_delete_cfp_talk', { 'cfp_id': 1, 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
