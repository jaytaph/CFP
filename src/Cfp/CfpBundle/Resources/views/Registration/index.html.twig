{% extends '::layout.html.twig' %}

{% block title %}Registration list{% endblock %}

{% block body %}
    <table class="list">
        <tr>
            <th>Registration</th>
            <th>Bio</th>
            <th>Talks submitted</th>
            <th>Action</th>
        </tr>
        {% for entity in entities %}
            <tr class="{{ cycle(['odd', 'even'], loop.index0) }}">
                <td>{{ entity.conference.name }}</td>
                <td>{{ entity.biography.description }}</td>
                <td align=center>{{ entity.submissions|length }}</td>
                <td>
                    [ <a href="{{ path('CfpCfpBundle_edit_registration', { 'id': entity.id }) }}">edit</a> ] -
                    [ <a href="{{ path('CfpCfpBundle_show_my_submissions', { 'id' : entity.id } ) }}">show submissions</a> ] -
                    [ <a href="{{ path('CfpCfpBundle_new_submission', { 'id' : entity.id } ) }}">New submission</a> ]
                </td>
            </tr>
        {% endfor %}
    </table>

{% endblock %}