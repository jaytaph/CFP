<h1>Cfp creation</h1>

<form action="{{ path('CfpConferenceBundle_create_cfp') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('CfpUserBundle_show_my_cfps') }}">
            Back to the list
        </a>
    </li>
</ul>
